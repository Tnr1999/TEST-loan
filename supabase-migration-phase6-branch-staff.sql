-- ═══════════════════════════════════════════════════════════
-- Phase 6: พนักงานเจ้าของบ้าน (branches.staff_id)
--   ใช้เป็นเจ้าของค่าแรง 20% ของบ้านนี้ — แม้หัวหน้ากอง/หัวหน้าสาย
--   เป็นคนกดรับเงินแทน ค่าแรงยังเข้าพนักงานเจ้าของบ้าน
--   ไม่ได้ตั้ง = fallback เป็นคนกดรับเงิน (recorded_by) แบบเดิม
-- ═══════════════════════════════════════════════════════════

alter table branches add column if not exists staff_id uuid references users(id);
