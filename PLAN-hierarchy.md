# แผนปรับโครงสร้างเป็นลำดับชั้น: กอง → บ้าน → ลูกค้า

> เอกสารวางแผน (ยังไม่ลงมือแก้โค้ด) — ใช้ยืนยันแนวทางก่อนเริ่มทำจริง

## 1. เป้าหมาย

ปรับระบบจากเดิม 2 ชั้น (สาขา → ลูกค้า) เป็น 3 ชั้น:

```
เจ้าของ
 └── กอง        (4 กอง)
      └── บ้าน        (30–50 บ้าน/กอง)
           └── ลูกค้า   (50–100 คน/บ้าน)
```

ขนาดข้อมูลโดยประมาณ: 4 × 40 × 75 ≈ **12,000 ลูกค้า**

## 2. สิทธิ์การเข้าถึง (3 ระดับ)

| บทบาท | เห็นอะไร | ทำอะไรได้ |
|--------|----------|-----------|
| **เจ้าของ (owner)** | ทุกกอง ทุกบ้าน | จัดการทั้งหมด: กอง/บ้าน/ผู้ใช้/ลูกค้า |
| **หัวหน้ากอง (head)** | เฉพาะกองตัวเอง (ทุกบ้านในกอง) | จัดการบ้าน + ลูกค้าในกองตัวเอง, บันทึกชำระ |
| **พนักงาน (staff)** | เฉพาะบ้านที่ได้รับมอบหมาย | บันทึกชำระ + ดูลูกค้าในบ้านที่ดูแล |

## 3. การเปลี่ยนฐานข้อมูล (Supabase)

### 3.1 ตารางใหม่: `groups` (กอง)
```sql
create table groups (
  id uuid primary key default gen_random_uuid(),
  name text not null,
  created_at timestamptz default now()
);
```

### 3.2 แก้ตาราง `branches` (= "บ้าน")
เพิ่มคอลัมน์ `group_id` เพื่อบอกว่าบ้านนี้อยู่กองไหน
```sql
alter table branches add column group_id uuid references groups(id);
```
> หมายเหตุ: ใช้ตาราง `branches` เดิมเป็น "บ้าน" เพื่อลดการรื้อโค้ด — ภายในเรียก "บ้าน" แต่ schema ยังชื่อ branches

### 3.3 ตารางสิทธิ์
- คงเดิม `user_branches` (พนักงาน ↔ บ้านที่ดูแล)
- เพิ่มใหม่ `user_groups` (หัวหน้ากอง ↔ กองที่ดูแล)
```sql
create table user_groups (
  user_id uuid references users(id),
  group_id uuid references groups(id),
  primary key (user_id, group_id)
);
```

### 3.4 `customers`, `daily_records`
ไม่ต้องแก้ — `customers.branch_id` ยังชี้ไปที่ "บ้าน" เหมือนเดิม

## 4. การเปลี่ยน Role
```
ปัจจุบัน: owner / manager / staff
ใหม่:     owner / head (หัวหน้ากอง) / staff
```
- `manager` เดิม → แมปเป็น `head` (หรือคงไว้ก็ได้ ตกลงกันอีกที)

## 5. การเปลี่ยนหน้าจอ (UI)

| หน้า | สิ่งที่เปลี่ยน |
|------|----------------|
| **เมนูใหม่ "กอง"** | สร้าง/แก้/ลบ 4 กอง (เจ้าของเท่านั้น) |
| **หน้าสาขา → "บ้าน"** | เพิ่ม dropdown เลือกกอง, จัดกลุ่มบ้านตามกอง |
| **ฟอร์มเพิ่มลูกค้า** | เลือกกอง → กรองบ้าน → เลือกบ้าน |
| **ตัวกรองแดชบอร์ด** | กรอง 2 ชั้น: เลือกกอง → เลือกบ้าน |
| **หน้าจัดการผู้ใช้** | head = เลือกกองที่ดูแล, staff = เลือกบ้านที่ดูแล |
| **คำในระบบ** | "สาขา" → "บ้าน" ทุกที่ + เพิ่มคำว่า "กอง" |

## 6. ⚠️ ประเด็น Performance (สำคัญที่สุด)

**ปัญหา:** โค้ดปัจจุบัน (`loadAll()`) โหลด customers + daily_records **ทั้งหมด** เข้าเครื่องทุกครั้ง
- 12,000 ลูกค้า + ประวัติรายวันสะสม (อาจถึงหลักแสน–ล้านแถว/ปี)
- จะทำให้ **โหลดช้ามาก / ค้าง / กินเน็ต** บนมือถือ

**ทางแก้ (ต้องทำควบคู่):**
1. โหลดแบบมีเงื่อนไข — ดึงเฉพาะกอง/บ้านที่เลือก ไม่ดึงทั้งหมด
2. `daily_records` ดึงเฉพาะช่วงวันที่ดูอยู่ (เช่น เดือนปัจจุบัน) ไม่ดึงทั้งหมด
3. ใส่ pagination ในหน้ารายชื่อลูกค้า
4. สรุปยอด (ดอก/ค่าแรง) คำนวณฝั่ง Supabase (RPC/view) แทนดึงดิบมานับในเครื่อง

## 7. แผนทำเป็นเฟส (แนะนำ)

**เฟส 1 — โครงสร้าง (ทำก่อน)**
- สร้างตาราง `groups`, เพิ่ม `group_id` ใน branches
- เมนูจัดการกอง + ผูกบ้านเข้ากอง
- ตัวกรองกอง→บ้าน ในแดชบอร์ด/ลูกค้า

**เฟส 2 — สิทธิ์**
- เพิ่ม role `head` + ตาราง `user_groups`
- ปรับ `myBranchIds()` ให้รวมบ้านจากกองที่ดูแล
- ปรับหน้าจัดการผู้ใช้

**เฟส 3 — Performance (ขาดไม่ได้เมื่อข้อมูลเยอะ)**
- เปลี่ยน loadAll() เป็นโหลดแบบมีเงื่อนไข + ตามช่วงวัน
- pagination + สรุปยอดฝั่ง server

## 8. ลูกค้า 1 คน กู้หลายที่ (สำคัญ)

### กฎธุรกิจ (ยืนยันแล้ว)
1. ลูกค้า 1 คน กู้ได้ **ไม่เกิน 2 กอง**
2. **ภายในกองเดียวกัน กู้ได้แค่ 1 บ้าน** (อยู่กองไหนแล้ว ห้ามกู้บ้านอื่นในกองนั้น)
3. → สูงสุด 1 คน = **2 สัญญา** (ต้องคนละกอง)

### ปัญห ากับระบบปัจจุบัน
ตอนนี้ `1 ลูกค้า = 1 แถว = 1 สัญญา` (ข้อมูลคน + เงินกู้ ปนกันในแถวเดียว)
→ ถ้าคนเดียวกู้ 2 ที่ ต้องสร้าง 2 แถว ชื่อ/เบอร์/บัตรซ้ำ และ**ระบบเช็คกฎข้างบนไม่ได้**

### ทางเลือก
**แนวที่ 1 — แยก "คน" (persons) ออกจาก "สัญญา" (loans)** ⭐ แนะนำ
```
persons (คน)         → 1 แถว/คน: ชื่อ, เบอร์, บัตรประชาชน
 └── loans (สัญญา)    → ผูก person_id + branch_id (บ้าน)
      ├── principal, remaining_principal, rate, interval, status
      └── daily_records → ผูก loan_id (แทน customer_id)
```
- ✅ บังคับกฎ "ไม่เกิน 2 กอง / 1 บ้านต่อกอง" ได้จริง (ตรวจตอนสร้างสัญญา)
- ✅ ดูภาพรวมคนเดียวได้ว่ากู้กี่ที่ ค้างรวมเท่าไหร่
- ❌ ต้องรื้อ schema + โค้ดเยอะ (customer → person + loan ทุกที่)

**แนวที่ 2 — คงโครงเดิม ผูกด้วยเลขบัตรประชาชน**
- ยัง 1 แถว = 1 สัญญา แต่ใช้บัตร ปชช. เป็นตัวเชื่อมว่าเป็นคนเดียวกัน
- ✅ แก้น้อย  ❌ ข้อมูลคนซ้ำหลายแถว, เช็คกฎได้แต่เปราะ (พึ่งความถูกต้องของเลขบัตร)

> หมายเหตุ: เพราะมีกฎจำกัดการกู้ (ข้อ 1–3) **แนวที่ 1 เหมาะกว่า** เพราะระบบต้องรู้ว่าเป็นคนเดียวกันถึงจะบังคับกฎได้

## 9. สิ่งที่ต้องตัดสินใจเพิ่มก่อนเฟส 1
- [ ] ค่าธรรมเนียม (`fee_per_person`) อยู่ระดับบ้าน หรือระดับกอง?
- [ ] ลำดับลูกค้า (seq) นับแยกในแต่ละบ้าน หรือทั้งกอง?
- [ ] "manager" เดิมจะเปลี่ยนเป็น "head" หรือเก็บไว้ทั้งคู่?
- [ ] มีข้อมูลลูกค้าจริงในระบบแล้วหรือยัง (มีผลต่อการ migrate)?
- [ ] เรื่องกู้หลายที่: เลือกแนวที่ 1 (แยก persons/loans) หรือแนวที่ 2 (ผูกบัตร)?
